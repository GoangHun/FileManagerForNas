# 개발일지: 2025-11-07

## 작업 내용
- 프로젝트 초기 설정 및 기본 구조 생성
  - `frontend`, `backend` 디렉터리 생성
  - `README.md`, `doc/요구사항정의서.md`, `doc/개발일지_2025-11-07.md` 파일 생성
- 개발 계획 수립
  - 프론트엔드: React (TypeScript), Bootstrap
  - 백엔드: Python, FastAPI

## 추가된 작업 내용
- Python 가상 환경 생성
  - 명령어: `python -m venv .venv`
- `.gitignore` 파일 생성 및 설정
  - 명령어: `write_file` (내용: `.venv/`, `node_modules/` 등)
- React 프론트엔드 프로젝트 초기화
  - 명령어: `npx create-react-app . --template typescript` (frontend 디렉터리 내에서 실행)
- FastAPI 백엔드 `main.py` 파일 생성
  - 명령어: `write_file` (내용: FastAPI 기본 코드)
- FastAPI 및 Uvicorn 패키지 설치 (가상 환경 내)
  - 명령어: `C:\Users\parkkh\projects\AIFileManagement\.venv\Scripts\python.exe -m pip install fastapi uvicorn`
- Git 저장소 초기화 및 GitHub 업로드
  - 명령어: `git init`
  - 명령어: `git add .`
  - `frontend` 디렉터리 내 중첩된 `.git` 폴더 제거 및 Git 추적 문제 해결
    - 명령어: `Remove-Item -Recurse -Force frontend/.git`
    - 명령어: `git rm --cached frontend`
    - 명령어: `git add frontend`
  - 명령어: `git commit -m "Initial project setup"`
  - 명령어: `git remote add origin https://github.com/GoangHun/FileManagerForNas.git`
  - 명령어: `git push -u origin master` (사용자가 직접 실행)

## 다음 계획
- 프론트엔드와 백엔드 간의 통신 설정 및 연동
- NAS 파일 시스템 연동을 위한 백엔드 기능 구현 시작
- AI 기능 통합을 위한 초기 설계
